#include <stdio.h>
#include "main.h"

/**
 * print_numbers - Prints the numbers from 0 - 9
 * Returns: void.
 */

void print_numbers(void)
{
	int i = 0;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
}
